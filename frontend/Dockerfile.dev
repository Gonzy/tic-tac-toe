FROM node:18-alpine

WORKDIR /app

# Copy package files first for better caching
COPY package*.json ./

# Install dependencies
RUN npm install

# Expose the port the React app runs on
EXPOSE 3000

# Command to run the React development server with hot reload
CMD ["npm", "start"]